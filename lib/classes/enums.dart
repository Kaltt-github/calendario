enum FromType { app, google, owned, shared }

enum EventType { father, anticipation, reminder, repeat, postposition }

enum DelayType { year, month, week, day, hour, minute, not }

enum Icon { calendar }

enum Laguage { spanish }

enum Theme { light, night }

enum Zone { argentina }
